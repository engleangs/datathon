name: nz_court_intelligence
version: "1.0.0"
config-version: 2

profile: nz_court_intelligence

model-paths: ["models"]
test-paths: ["tests"]

models:
  nz_court_intelligence:
    staging:
      +materialized: view
      +schema: staging

    intermediate:
      +materialized: view
      +schema: intermediate

    marts:
      +materialized: view
      +schema: analytics
