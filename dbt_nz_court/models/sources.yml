version: 2

sources:
  - name: raw
    database: "{{ env_var('SNOWFLAKE_DATABASE', 'NZ_COURT_INTELLIGENCE') }}"
    schema: RAW

    tables:
      - name: documents
        description: Metadata for uploaded public court judgment PDFs.

      - name: case_extractions
        description: Raw structured Bedrock/Strands extraction and deterministic validation payloads.
